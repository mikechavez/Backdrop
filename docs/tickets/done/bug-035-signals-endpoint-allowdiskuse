---
ticket_id: BUG-035
title: Signals Endpoint Aggregation Missing allowDiskUse
priority: MEDIUM
severity: MEDIUM
status: COMPLETED
date_created: 2026-02-23
date_completed: 2026-02-23
branch: fix/bug-035-signals-endpoint-allowdiskuse
commit: 65c968e
pr: 180
effort_estimate: 10 minutes
actual_effort: 10 minutes
---

# BUG-035: Signals Endpoint Aggregation Missing allowDiskUse

## Problem Statement

The `get_recent_articles_for_entity()` function in the signals API endpoint uses an aggregation pipeline (added in BUG-032) that includes `$sort` and `$group` stages without `allowDiskUse=True`. This pipeline has not yet hit the 32MB memory limit, but will as data volume grows — the same root cause as BUG-034.

### Root Cause

The BUG-032 fix added a `$group` + `$sort` deduplication stage to the aggregation pipeline in `signals.py`, but did not include `allowDiskUse=True`. This is a ticking time bomb — the same class of bug that crashed the signals page in BUG-034.

---

## Solution

Add `allowDiskUse=True` to all `.aggregate()` calls in the signals endpoint file.

### Changes Required

**File:** `src/crypto_news_aggregator/api/v1/endpoints/signals.py`

**`get_recent_articles_for_entity()` — BUG-032 pipeline**
```python
# Before
cursor = collection.aggregate(pipeline)

# After
cursor = collection.aggregate(pipeline, allowDiskUse=True)
```

Audit the full file for any other `.aggregate()` calls and apply the same fix.

---

## Verification

### ✅ Code Review Complete - 2026-02-25

**Investigation Summary:**

1. **Reviewed all aggregations in signals flow:**
   - `src/crypto_news_aggregator/api/v1/endpoints/signals.py`:
     - Line 207: `mentions_collection.aggregate(pipeline, allowDiskUse=True)` ✅
     - Line 259-264: `db.narratives.aggregate([...], allowDiskUse=True)` ✅

   - `src/crypto_news_aggregator/services/signal_service.py`:
     - Line 144: `collection.aggregate(pipeline, allowDiskUse=True)` ✅
     - Line 303: `db.entity_mentions.aggregate(pipeline, allowDiskUse=True)` ✅
     - Line 635: `db.entity_mentions.aggregate(pipeline, allowDiskUse=True)` ✅
     - Line 736: `db.entity_mentions.aggregate(pipeline, allowDiskUse=True)` ✅
     - Line 743-748: `db.narratives.aggregate([...], allowDiskUse=True)` ✅

2. **Parameter Syntax Verified:**
   - All aggregations use correct camelCase: `allowDiskUse=True` (NOT snake_case)
   - Motor 3.5.0 (in pyproject.toml) supports this parameter
   - All aggregations with `$sort` or `$group` stages are protected

3. **Codebase Audit - Other aggregations found WITHOUT allowDiskUse:**
   - `src/crypto_news_aggregator/llm/cache.py`: Lines 256, 281 (have `$sort`)
   - `src/crypto_news_aggregator/api/admin.py`: Lines 62, 123, 190, 245, 311, 331 (some have `$sort`)
   - `src/crypto_news_aggregator/api/v1/endpoints/articles.py`: Lines 202, 286, 347 (all have `$sort`)
   - `src/crypto_news_aggregator/services/article_service.py`: Lines 372, 451 (have `$sort`)
   - `src/crypto_news_aggregator/db/operations/narratives.py`: Line 439
   - `src/crypto_news_aggregator/db/operations/entity_mentions.py`: Line 168

   **Status:** These are NOT on signals endpoint path, but TASK-011 should audit them

4. **Conclusion:**
   - ✅ BUG-035 fix is COMPLETE and CORRECT in signals.py
   - ✅ BUG-034 fix is COMPLETE and CORRECT in signal_service.py
   - ✅ All signals endpoint aggregations are protected
   - ⚠️ Error persistence likely due to:
     a) Stale deployment not picking up code changes
     b) Railway deployment misconfiguration
     c) Caching/old build artifacts

**Next Steps:**
1. Verify Railway deployment actually pulled the latest code (check commit hash)
2. Clear any build caches on Railway
3. Force redeploy from main branch
4. Monitor logs for error location clarity

---

## Impact

- ✅ **Preventive fix**: Avoids future BUG-034-class crash in this endpoint
- ✅ **No behavior change**: `allowDiskUse=True` only affects MongoDB's sort strategy when memory is exceeded
- ✅ **Deployed**: PR #180 created and merged alongside BUG-034

---

## Implementation Details

**Changes Made:**
- Line 207: `mentions_collection.aggregate(pipeline, allowDiskUse=True)`
- Line 264: `db.narratives.aggregate([...], allowDiskUse=True)`

**Files Changed**

- `src/crypto_news_aggregator/api/v1/endpoints/signals.py` — aggregate calls updated (2 changes)

## Related Tickets

- BUG-032: Added the deduplication pipeline that needs this fix
- BUG-034: Same class of bug, already hit in `signal_service.py` (merged)
- TASK-011: Codebase-wide audit of all `.aggregate()` calls